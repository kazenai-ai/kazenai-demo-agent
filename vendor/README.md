# Vendored wheels (CI)

Unpublished deps for hermetic CI (no private sibling checkout).

| Wheel | Purpose |
|-------|---------|
| `kazen_event_schema-0.6.0-py3-none-any.whl` | Event schema |
| `kazenai_contracts-0.1.0-py3-none-any.whl` | Contracts |
| `kazenai-1.0.1-py3-none-any.whl` | Core SDK |

Refresh from sibling builds under `../kazen-event-schema`, `../kazenai-contracts`, `../kazenai-core`.
